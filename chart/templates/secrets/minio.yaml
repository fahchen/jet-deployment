{{- if .Values.minioTLSSecret | empty | not }}
{{- with .Values.minioTLSSecret }}
{{- if .certificate | empty }}
{{- fail "Please provide `certificate` for minioTLSSecret" }}
{{- end }}
{{- if .key | empty }}
{{- fail "Please provide `key` for minioTLSSecret" }}
{{- end }}

apiVersion: v1
kind: Secret
metadata:
  name: {{ include "jet-chart.fullname" $ }}-mimio-tls-secret
  labels:
    {{- include "jet-chart.labels" $ | nindent 4 }}
type: kubernetes.io/tls
data:
  tls.crt: {{ .certificate }}
  tls.key: {{ .key }}
{{- end }}
{{- end }}
